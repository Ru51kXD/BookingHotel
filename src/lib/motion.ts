'use client';

// Импортируем только необходимые компоненты из framer-motion
import {
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  animate,
  m,
  LazyMotion,
  domAnimation,
  useScroll,
  useInView,
  useMotionValueEvent,
  useSpring,
  easeInOut,
  easeIn,
  easeOut,
  useAnimationFrame
} from 'framer-motion';

// Переэкспортируем их через именованный экспорт
export {
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  animate,
  m,
  LazyMotion,
  domAnimation,
  useScroll,
  useInView,
  useMotionValueEvent,
  useSpring,
  easeInOut,
  easeIn,
  easeOut,
  useAnimationFrame
}; 